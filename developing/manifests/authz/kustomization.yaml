apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeflow-workspaces

resources:
- ../../../workspaces/authz/manifests/kustomize/base

components:
- ../../../workspaces/authz/manifests/kustomize/components/gateway-api

patches:
- path: deployment_patch.yaml

configMapGenerator:
- name: workspaces-authz-config
  behavior: replace
  envs:
  - params.env
