apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
- reference-grant.yaml

patches:
- path: deployment_patch.yaml

configMapGenerator:
- name: kubeflow-workspaces-ext-authz-config
  envs:
  - params.env
